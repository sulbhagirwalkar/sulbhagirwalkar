public class Demo {
    List<Payment> input = new List<>();
    input.stream()
            .mapToObj(Collotors.ToMap());
    Map<String, List<Payment>> result = new HashMap<>();

    result.stream
        .filter(id -> Payment.getId())
            .mapToInt(Integre::getInt)
	.forEach(System.out::println);


    public static void main(String[] args) {
        List<Payments> input= new List<>();
        Map<String, list<Payments>> result = new Hashmap<>();
        result.put("Saving", new List("Payment", "SubscrptionTYpe", 15));

        input.stream().mapToObj(Collotors.ToMap());


    }
}
