/**
 * The regex .*[].* in Java is used to check if a string contains square brackets, [ and ]. Here's how it works:
 * .: Matches any character (except for line terminators by default).
 * *: Matches the preceding element zero or more times.
 * []: Defines a character set. Inside the square brackets, special characters lose their special meaning, except for ^, -, and ]. However, [ does not need to be escaped within [].
 * .*: Matches any sequence of characters (including an empty sequence) before the square brackets.
 * []: Matches either [ or ].
 * .*: Matches any sequence of characters (including an empty sequence) after the square brackets.
 * */

public class GetTheBestRestaurantOfCity {

    public static String getBestRestaurantInTheCity(String city) throws Exception {

        return null;
    }
    public static void main(String[] args) {

    }
}
